{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:pto-isa:agentic-circuit:schema:capabilities:0.2",
  "title": "Agentic Circuit Capabilities 0.2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "version", "contract_epoch", "contract_identities", "items", "compiler_build_id", "runtime_build_id"],
  "properties": {
    "schema": {"const": "agentic-circuit-capabilities"},
    "version": {"const": "0.2"},
    "contract_epoch": {"const": "0.2"},
    "contract_identities": {
      "type": "object",
      "additionalProperties": false,
      "required": ["acpy", "acir", "acsim", "cli", "component_schema", "cxx_source_contract", "pto_trace", "diagnostic", "build_manifest", "run_manifest", "run_result"],
      "properties": {
        "acpy": {"const": "acpy@0.2"},
        "acir": {"const": "acir@0.2"},
        "acsim": {"const": "acsim@0.2"},
        "cli": {"const": "agentic-circuit-cli@0.2"},
        "component_schema": {"const": "agentic-circuit-component@0.2"},
        "cxx_source_contract": {"const": "gfsim-cxx20@0.2"},
        "pto_trace": {"const": "pto-trace@0.2"},
        "diagnostic": {"const": "agentic-circuit-diagnostic@0.2"},
        "build_manifest": {"const": "agentic-circuit-build-manifest@0.2"},
        "run_manifest": {"const": "agentic-circuit-run-manifest@0.2"},
        "run_result": {"const": "agentic-circuit-run-result@0.2"}
      }
    },
    "items": {"type": "array", "items": {"$ref": "#/$defs/item"}},
    "compiler_build_id": {"type": "string", "minLength": 1},
    "runtime_build_id": {"type": "string", "minLength": 1}
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "name", "availability", "schema_fingerprint", "implementation_fingerprint"],
      "properties": {
        "kind": {"enum": ["provider", "component", "policy", "interface", "protocol", "output_format"]},
        "name": {"type": "string", "minLength": 1},
        "availability": {"enum": ["available", "declared_unavailable"]},
        "schema_fingerprint": {"$ref": "#/$defs/sha256"},
        "implementation_fingerprint": {"oneOf": [{"type": "null"}, {"$ref": "#/$defs/sha256"}]}
      }
    }
  }
}
